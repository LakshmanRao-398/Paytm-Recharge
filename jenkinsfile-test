pipeline {
    agent any

    stages {
        stage('Checkout from GitHub') {
            steps {
                git url: 'https://github.com/LakshmanRao-398/testing.git', branch: 'test'
            }
        }

        stage('Build') {
            steps {
                echo 'Running build...'
                // Add your build steps here
            }
        }

        stage('Post-Build') {
            steps {
                echo 'Done!'
            }
        }
    }
}
